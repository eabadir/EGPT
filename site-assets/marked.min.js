// Marked.js loader — loads from CDN
(function() {
  if (typeof marked !== 'undefined') return;
  var script = document.createElement('script');
  script.src = 'https://cdn.jsdelivr.net/npm/marked@12/marked.min.js';
  document.head.appendChild(script);
})();
